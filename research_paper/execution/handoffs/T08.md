# T08 in-progress handoff — conventional FAST-LIO diagnostic

24 September 2026. **RUNNING, not accepted.** Stop point: the user requested a pause and a GitHub push during the final 55-second export rerun; that run was terminated and its partial files must not be treated as a result.

## What exists

- Dependencies inspected: T02 closest-work comparison, T05 indicator/data contract, T06 pinned FAST-LIO source and Hilti development configuration. Reference/error streams were not inputs to the indicator.
- [Derivation and source mapping](../../protocol/FASTLIO_INFORMATION_DIAGNOSTIC.md): point-to-plane measurement-only 6×6 information from accepted map correspondences, with explicit 1/3/5 m rotational lever scales, `LASER_POINT_COV = 0.001`, and no posterior/accuracy interpretation. This is not X-ICP or SuperLoc.
- [Python mirror](../../experiments/src/fastlio_information.py) and [four fixtures](../../experiments/tests/test_fastlio_information.py): analytic Jacobian, one plane, three planes, missing correspondences. Full experiment suite passed 28/28.
- [Optional backend patch](../../experiments/patches/fast_lio_health_diagnostic.patch): applies to pinned FAST-LIO revision `7cc4175de6f8ba2edf34bab02a42195b141027e9` after the existing C++17 build patch. `git apply --reverse --check` succeeded on the instrumented ignored checkout. It built with catkin; no build errors. The final patch adds unavailable rows for first scan, empty undistorted cloud, map initialization and too-few downsampled points. **This final version has not had a complete replay.**
- [Replay script](../../experiments/run_hilti_health_smoke.sh) accepts workspace, new output directory, duration and `on|off`. It refuses an existing run directory. The Acer source partition remained read-only; build and transient outputs were under a temporary `/tmp/fastlio-health-*` workspace. Do not depend on that path surviving reboot.

## Preliminary checks, not final evidence

- With an earlier compiled patch, 12-second export-on and export-off replays each captured 116 poses; pose CSV SHA-256 was identical (`514796d20aac61d5d6abd3b6cce94c6664d75b0f81f580fc472fe58ec9d28d1d`). The 12-second export had 116 complete timestamp groups after fixing per-scan flush. No estimator-output change was observed in this prefix.
- A 55-second replay **before** explicit initialization-unavailable rows recorded 546 valid diagnostic timestamp groups × three scales, from 551 delivered LiDAR clouds. Its 546 pose timestamps matched the original development replay, and maximum position/quaternion component difference was below `3.8e-13`. The five startup clouds lacked diagnostic rows in that preliminary version; the later patch addresses this, but needs the clean rerun.
- The attempted final 55-second rerun was interrupted and terminated at the user's request. Do not count its partial CSV, make a before/after indicator claim, or mark T08 DONE.

## Exact next checks

1. Mount Acer read-only; establish the isolated ROS environment and a fresh workspace. Apply both versioned backend patches to the pinned checkout, build, and run the [script](../../experiments/run_hilti_health_smoke.sh) with `55 on` into a new ignored/temp directory. Confirm patch application and configuration in a manifest.
2. Confirm 551 adapter-published clouds versus diagnostic timestamp groups, three complete 12-column rows per group, unavailable reasons for every skipped startup scan, monotonically increasing timestamps, no duplicate scales, finite ascending eigenvalues on valid rows, and pose parity against the original or a final-patch `off` replay.
3. Record output SHA-256, source revision and dirty patch hashes, effective configuration, environment, timestamps/runtime/RSS, warning counts and rejected clouds. Retain a small reviewed evidence table. Do not fit a threshold to the scene or use the reference path in the online process.
4. Only then finish the T08 acceptance checklist and proceed to an honest T09 pilot showing missing reference intervals. R1 remains a no-go for Hilti-only recovery inference.

The [current handoff](../CURRENT_HANDOFF.md) and [status ledger](../STATUS.md) are the entry points for resuming.
