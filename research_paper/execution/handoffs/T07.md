# T07 handoff — offline trajectory evaluation

**Successor note (24 September 2026): T07 DONE as an offline-evaluation task.** A [LiDAR-only structural audit](../../data/HILTI_EXP18_EVENT.md) fixed an Exp18 **exit-only** scene event before errors were summarized. The [development-only T07 pilot](../../evidence/HILTI_EXP18_T07_PILOT.md) reports the complete 1,092-row error stream, input/output hashes, 24 passing experiment tests, explicit reference-gap counts and a pre-exit (not pre-entry) alignment. The evaluator accepts either a genuine entry time or a separately named pre-exit alignment target. The [frame audit](../../data/HILTI_EXP18_FRAME_AUDIT.md) and [metadata](../../data/hilti_exp18_reference_metadata.json) support only a qualified common-body comparison, not independent truth. The scientific gate is **not** cleared: 13/56 matched post-exit 1 s windows and 0/56 3 s windows are valid, so no sustained recovery result exists. Follow the [current handoff](../CURRENT_HANDOFF.md). The GEODE blocker below is historical failure evidence, not the live T07 state.

## Status

**Historical GEODE state: BLOCKED on real-data acceptance** (23 September 2026). The offline evaluator and eight independent mathematical fixtures passed. Canonical GEODE error and recovery values remain unavailable because the released `Urban_Tunnel01` reference does not unambiguously identify its tracked body frame or provide the needed direction-explicit conversion to FAST-LIO's output body. The two T06 replays also grossly under-track motion. No identity body transform, scale fit, post-event alignment, or invented ground truth was used.

## Artifacts and checks

- `experiments/src/trajectory_eval.py`: offline-only reference association, SE(3) local translation/rotation error, fixed pre-entry alignment, accumulated error, rate fields, and explicit unavailable reasons. It has no ROS or backend imports. Its CLI requires a metadata file with `verified: true` before accepting a body transform.
- `experiments/tests/test_trajectory_eval.py`: zero trajectory, analytic translation/rotation increment, constant world-frame change, reset boundary, reference gap/extrapolation/ambiguity, quaternion sign, metric-scale error, and decimal timestamp rounding. `python3 -m unittest discover -s research_paper/experiments/tests -v` passes 8/8.
- Ignored row-level outputs: `experiments/generated/runs/T06_full/evaluation.csv` (5,704 rows, 2,616 rows with timestamp-associated reference, zero formal local errors) and `experiments/generated/runs/T06_event_local/evaluation.csv` (3,106 rows, 1,448 with timestamp-associated reference, zero formal local errors). Each estimate produces one row for each provisional 1 s and 3 s window. `REFERENCE_BODY_UNKNOWN`, `REFERENCE_GAP`, `REFERENCE_UNCOVERED`, and `WINDOW_INCOMPLETE` remain explicit.
- The released reference has eight sorted-timestamp gaps exceeding the provisional 0.20 s interpolation maximum and 643 adjacent out-of-order source-row timestamp decreases. The evaluator never interpolates across one of these gaps. Exact matches at a gap endpoint are retained, but relative windows crossing the gap are rejected.

The earlier GEODE command now additionally requires `--event-id UT01_TUNNEL2 --entry-start-ns 1693022176719765425`; keep its body transform omitted so errors remain unavailable. Do not rerun it merely to replace the historical artifact. The reference is read only by this offline process.

## Historical GEODE acceptance blocker and repair boundary

T07 acceptance calls for valid and invalid real-data error counts, not just a mathematically tested evaluator. GEODE's valid count is zero by design while its reference body remains unknown. The evaluator repairs have been tested but are not yet a real-data T07 acceptance result. Its source-order reference segmentation is conservative: a backwards timestamp jump starts a new coverage segment, and overlapping segments become ambiguous rather than being stitched together. Hilti's 20 segments/19 gaps are recorded in metadata. The candidate 32–36 s transition must be frozen from scene geometry before event-anchored errors are calculated.

The next scientific choice is whether to obtain documented reference/body calibration and repair the GEODE/FAST-LIO pairing, or switch the development candidate to a sequence with explicit ground-truth body/clock/calibration and a demonstrated weak-to-rich transition. A backend change alone cannot establish GEODE's missing reference convention. Keep both failed T06 runs in the ledger. Do not proceed to a recovery plot or pass R1 using these unavailable errors.

## Current T07 acceptance audit (24 September)

- **Dependencies inspected:** T05 contracts, T06 pinned FAST-LIO Hilti smoke output, actual Hilti reference and calibration, source-order reference gaps, LiDAR-only event audit, and the raw-gyro frame audit.
- **Files changed:** `experiments/src/trajectory_eval.py` and its tests; `experiments/src/summarize_hilti_pilot.py` and its test; `protocol/HILTI_PILOT_ANALYSIS.md`; `evidence/HILTI_EXP18_T07_PILOT.md`; sequence-specific reference metadata. The event-selection script/protocol and CSV are linked from that pilot.
- **Sources/revisions:** pinned FAST-LIO `7cc4175de6f8ba2edf34bab02a42195b141027e9`; pose/reference SHA-256 and all exact invocation arguments are in the pilot. The current repository revision/dirty state must be captured at a later freeze; this run is explicitly exploratory, not a frozen result.
- **Checks:** 24/24 experiment tests passed in the recorded compatible isolated environment. The Hilti replay produced 546 pose timestamps × two provisional windows = 1,092 rows, retaining valid/invalid reasons. Entire replay: 1 s has 422 valid/124 invalid; 3 s has 344 valid/202 invalid. A single pre-exit anchor was used; no post-event realignment, reset bridging, scale fitting or invented interpolation. Exact commands and output hash are in the pilot.
- **Unresolved scientific limit:** matched post-exit coverage is 13/56 at 1 s and 0/56 at 3 s, and the publisher's dense reference partly depends on LiDAR registration. T07 is complete as specified software/evaluation work; T09/R1 must not translate this into a recovery claim.
- **Next eligible task:** T08's actual backend indicator, small-file alternative-reference screening, then an honest T09 availability timeline and R1 gate.
